

<div class="product-item text-center my-2">
    <a href="{{ path('app_product', { 'slug' : product.slug}) }}">
        <img src="{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid product-img">
    </a>

    <h5 class="mt-2">{{ product.name }}</h5>
    <span class="product-subtitle">{{ product.subtitle }}</span>
    <span class="product-price"><b>{{ (product.price / 100) | number_format(2, ',', '.') }} €</b></span>
</div>
